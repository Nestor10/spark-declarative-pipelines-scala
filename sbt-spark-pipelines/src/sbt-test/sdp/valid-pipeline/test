# F7b scripted: valid pipeline → manifest with expected canonical content
> sdpManifest
> checkManifest

# F14b: schema codegen — inferred shapes become named-tuple aliases,
# and the project still compiles WITH the generated file in sources.
> sdpImportSchemas
> checkSchemas
> compile

# Incremental correctness (D10 / classload-eval): dropping a dataset from the
# pipeline must drop its fragments from the next manifest WITHOUT a clean. The
# author removes Silver.orders from the pipeline list and deletes its source;
# recompile + re-eval reflects it. (The classpath-keyed cache invalidates on
# the changed compiled products.)
$ copy-file extra/WarehouseNoSilver.scala src/main/scala/pipelines/Warehouse.scala
$ delete src/main/scala/pipelines/Silver.scala
> sdpManifest
> checkManifestAfterDelete
